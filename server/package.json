{
  "name": "callme-mcp",
  "version": "2.0.1",
  "description": "Claude가 전화로 사용자에게 연락할 수 있게 하는 MCP 서버",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "prestart": "bun install --silent",
    "start": "bun run src/index.ts",
    "daemon": "bun run src/daemon.ts",
    "dev": "bun --watch src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@ngrok/ngrok": "^1.4.1",
    "openai": "^4.77.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/ws": "^8.5.13",
    "bun-types": "latest"
  }
}
